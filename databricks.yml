bundle:
  name: retail_project

workspace:
  root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.environment}

resources:
  jobs:
    retail_medallion_pipeline:
      name: "Retail Medallion Pipeline"
      
      # FIX 1: Singular 'trigger'
      trigger:
        periodic:
          cron: "0 0 0 * * ?" 
          timezone_id: "UTC"

      tasks:
        - task_key: bronze_ingestion
          spark_python_task:
            python_file: ./src/Bronze.py
            parameters: 
              - ./resources/retail_pipeline.yml
          # FIX 2: Moved job_cluster_key out of a compute block
          job_cluster_key: serverless_compute
            
        - task_key: silver_processing
          depends_on:
            - task_key: bronze_ingestion
          spark_python_task:
            python_file: ./src/Silver.py
            parameters: 
              - ./resources/retail_pipeline.yml
          job_cluster_key: serverless_compute

        - task_key: gold_aggregation
          depends_on:
            - task_key: silver_processing
          sql_task:
            # FIX 3: Changed 'query' to 'file' and 'file_path' to 'path'
            file:
              path: ./src/Gold.sql
            warehouse_id: ${resources.sql_warehouses.gold_serverless_warehouse.id}

      job_clusters:
        - job_cluster_key: serverless_compute
          new_cluster:
            spark_version: 14.3.x-scala2.12
            node_type_id: Standard_DS3_v2
            num_workers: 2 

  sql_warehouses:
    gold_serverless_warehouse:
      name: "Gold_Pipeline_Serverless"
      cluster_size: "2X-Small"
      enable_serverless_compute: true
      auto_stop_mins: 10